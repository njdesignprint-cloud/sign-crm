const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");
test("language switching translates static and newly rendered content both ways",()=>{const core=fs.readFileSync(path.join(__dirname,"../assets/js/18-i18n.js"),"utf8");const complete=fs.readFileSync(path.join(__dirname,"../assets/js/25-i18n-complete.js"),"utf8");const html=fs.readFileSync(path.join(__dirname,"../index.html"),"utf8");assert.equal(core.includes('new CustomEvent("crm-language-changed"'),true);assert.equal(complete.includes("MutationObserver"),true);assert.equal(complete.includes('lang()==="es"'),true);assert.equal(complete.includes('"Cotización":"Estimate"'),true);assert.ok(html.indexOf("18-i18n.js")<html.indexOf("25-i18n-complete.js"));});
