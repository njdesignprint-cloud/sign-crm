const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");
test("language switching translates static and newly rendered content both ways",()=>{const core=fs.readFileSync(path.join(__dirname,"../assets/js/18-i18n.js"),"utf8");const complete=fs.readFileSync(path.join(__dirname,"../assets/js/25-i18n-complete.js"),"utf8");const html=fs.readFileSync(path.join(__dirname,"../index.html"),"utf8");assert.equal(core.includes('new CustomEvent("crm-language-changed"'),true);assert.equal(complete.includes("MutationObserver"),true);assert.equal(complete.includes("reverseTranslations"),true);assert.equal(complete.includes("reverseAttributeTranslations"),true);assert.equal(complete.includes('"Cotización":"Estimate"'),true);assert.ok(html.indexOf("18-i18n.js")<html.indexOf("25-i18n-complete.js"));});

test("every static Spanish interface phrase is covered by an i18n catalog",()=>{const core=fs.readFileSync(path.join(__dirname,"../assets/js/18-i18n.js"),"utf8");const complete=fs.readFileSync(path.join(__dirname,"../assets/js/25-i18n-complete.js"),"utf8");const html=fs.readFileSync(path.join(__dirname,"../index.html"),"utf8");const catalog=core+complete;const spanish=/[áéíóúñ¿¡]|\b(Agregar|Guardar|Nuevo|Nueva|Buscar|Todos|Todas|Sin|Control|Fecha|Precio|Costo|Material|Trabajo|Cliente|Pago|Gasto|Estado|Nombre|Notas|Cerrar|Cancelar|Pendiente|Resumen|Configuración|Proveedor|Compra|Instalación|Producción|Estimado|Ganancia|Saldo|Cobrado|Cuenta|Usuario|Empresa|Correo|Teléfono|Dirección|Responsable|Prioridad|Tipo|Cantidad|Unidad|Concepto|Categoría|Referencia|Semana|Período|Aplicar|Puedes|Solo|Eliminar|Restaurar)\b/;const texts=[...html.matchAll(/>([^<>]+)</g)].map(match=>match[1].replace(/\s+/g," ").trim()).filter(text=>text&&spanish.test(text));const missing=[...new Set(texts)].filter(text=>!catalog.includes(text));assert.deepEqual(missing,[]);});
